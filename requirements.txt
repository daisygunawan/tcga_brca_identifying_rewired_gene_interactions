# requirements.txt
# Python packages required for the TCGA_BRCA analysis scripts.
# Ensure compatibility with Python 3.8+.
# To install, run: pip install -r requirements.txt

pyyaml>=6.0
numpy>=1.23,<2.0  # Pin to <2.0 for gseapy compatibility
pandas>=2.1
tqdm>=4.66
scipy>=1.12
networkx>=3.0
matplotlib>=3.8
seaborn>=0.13
statsmodels>=0.14
openpyxl>=3.1
python-louvain>=0.16  # Or community>=0.19
requests>=2.28
scikit-learn>=1.3.0  # For classification
plotly>=5.15  # For Sankey viz
certifi>=2023  # For API security
xgboost>=2.1.0  # For classification
setuptools>=65.0  # Required by gseapy for pkg_resources
gseapy>=0.10.8  # Pathway enrichment and GSEA analysis
lightgbm>=4.6.0 # LightGBM – powerful non-linear meta-learner capable of capturing complex interactions between base predictions

# Install libomp via Homebrew (for xgboost on macOS)
# brew install libomp

# Installation notes:
# - numpy pinned to <2.0 for compatibility with gseapy 0.10.8
# - setuptools required for gseapy's pkg_resources in Python 3.12+
# - For gseapy 1.1+, use numpy>=2.0 (requires Rust compiler for source build)